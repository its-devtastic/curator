# Custom theme

You can customize the theme by providing a custom Ant Design `ThemeConfig`.
Read more about the available options [here](https://ant.design/docs/react/customize-theme).

## Example

```ts
const config = {
  theme: {
    token: {
      colorPrimary: "#FFCC00",
    },
  },
};
```
