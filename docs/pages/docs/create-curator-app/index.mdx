import { Callout } from "nextra/components";

# Create Curator App

<Callout type="info">You need at least Node 18</Callout>

The easiest way to bootstrap your Curator project is with the Create Curator App.

In your terminal run:

```shell
npx create-curator-app@latest
```

The CLI will guide you through the setup.
